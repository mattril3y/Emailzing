<UserControl x:Class="Emailzing.UI.Views.Content"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:Emailzing.UI.Views"
			 mc:Ignorable="d" 
			 d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<TextBlock Text="{Binding From}" Grid.Row="0"/>
		<TextBlock Text="{Binding To}" Grid.Row="1"/>
		<TextBlock Text="{Binding Title}" Grid.Row="2"/>
		<TextBlock Text="{Binding FullTime}" Grid.Row="3"/>
		<TextBlock TextWrapping="Wrap" Text="{Binding Content}" Grid.Row="4"/>
	</Grid>
</UserControl>
